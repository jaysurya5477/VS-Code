# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: com.sap.zmmgrndash
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          url: https://vhafbmedap01.hec.erp.alimco.in:443/
          client: '120'
          auth: basic
        credentials:
          username: env:med_user
          password: env:med_pass
        app:
          name: ZMM_GRN_DASH
          description: GRN Dashboard Fiori Analytical Application
          package: ZMM
          transport: MEDK913140
        exclude:
          - /test/
